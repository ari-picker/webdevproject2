# Quiz Generator - AI Full Stack Web App

A full-stack web application that generates multiple-choice quizzes on any topic using the Groq AI API (powered by Llama 3.3 70B). Built with Express.js, MongoDB, and Bootstrap 5.

## Features

- **User Authentication**: Sign up and sign in with scrypt password hashing
- **Session Management**: Persistent login via cookies (cookie-parser)
- **AI-Powered Quiz Generation**: Enter any topic and get a multi-question quiz generated by Llama 3.3 70B via Groq Cloud
- **Interactive Quiz Taking**: Select answers and get instant scoring with visual feedback
- **Responsive Design**: Bootstrap 5 dark theme, mobile-friendly

## Tech Stack

| Layer      | Technology      |
|------------|----------------|
| Backend    | Node.js, Express 4.16 |
| Frontend   | EJS templates, Bootstrap 5.3 |
| Database   | MongoDB Atlas  |
| AI API     | Groq Cloud (llama-3.3-70b-versatile) |

## How to Run

1. Clone the repository
2. Install dependencies: `npm install`
3. Create a `.env` file with `ATLAS_URI` and `GROQ_API_KEY`
4. Start the server: `npm start` (or `npm run dev` for development with nodemon)
5. Visit `http://localhost:3000`

## Database Schema

**Collection: `users`**
```json
{
  "name": "string",
  "email": "string",
  "password": "string (scrypt hashed)",
  "salt": "string (crypto.randomBytes)"
}
```

**Collection: `quiz_history`**
```json
{
  "userEmail": "string",
  "userName": "string",
  "topic": "string",
  "title": "string",
  "score": "number",
  "total": "number",
  "percentage": "number",
  "results": [
    {
      "question": "string",
      "options": ["string"],
      "correctIndex": "number",
      "userAnswer": "number",
      "correct": "boolean"
    }
  ],
  "timestamp": "Date"
}
```

## API Endpoints

| Method | Route               | Description          | Auth Required |
|--------|---------------------|----------------------|---------------|
| GET    | `/`                 | Redirect to /signup  | No |
| GET    | `/signup`           | Sign up page         | No |
| GET    | `/signin`           | Sign in page         | No |
| POST   | `/users/signup/submit` | Create account    | No |
| POST   | `/users/signin/submit` | Log in            | No |
| GET    | `/dashboard`        | Main quiz page       | Yes |
| POST   | `/quiz/generate`    | Generate quiz via AI | Yes |
| POST   | `/quiz/submit`      | Submit answers       | Yes |
| GET    | `/users/logout`     | Log out              | Yes |

## Deployment

The app is hosted on Render's free tier at `https://webdevproject2.onrender.com`.

## Test Credentials

1. Visit `https://webdevproject2.onrender.com`
2. Create an account at `/signup`
3. Sign in and generate a quiz

Or run locally:
1. `npm install`
2. Set up `.env` with `ATLAS_URI` and `GROQ_API_KEY`
3. `npm start`
4. Visit `http://localhost:3000`
